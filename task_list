PICKED TASKS

* REFACTORING
* formalize what is iterator, and what is an interface
* who ever creates an iterator are allowed to delete them
* interfaces are not allowed to delete iterator passed to them as input parameters
* jsonb_read_iterator should be an interface instead
* fix the api, fix tests and test accordingly, and make sure they pass and not cause double free

* JSONB
  * build jsonb_node from json_node and viceversa
  * add hasher to hash some attribute nestedly

materialized_numeric
  * add
  * sub
  * multiplication
  * division
  * modulus (only if is_integral returns true on divisor and dividend)