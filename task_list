PICKED TASKS

* revamp digit_read_iterator with new uval and datum like api, make this api private
* make numeric reader insterface to work with datum api
* simplify and refactor code for binary write iterator
* discard use of relative_positional_accessor, and embrace STATION_POSITION(0 or 1) instead, (for the reader side of the code only) for read functions
* discard binary_reader_interface completely

* make macros to represent positional_acessor for the extended_head_page_id, inline_value, and other numeric's attributes, refactor all of that in the _commons.h


* JSONB
  * build jsonb_node from json_node and viceversa
  * add hasher to hash some attribute nestedly

materialized_numeric
  * add
  * sub
  * multiplication
  * division
  * modulus (only if is_integral returns true on divisor and dividend)