PICKED TASKS

 * allow inserting char* extension_type, in all the type generation for the extended types
 * strcmp now becomes check prefix + check suffix
 * has extended type, now also takes in extension_type, to check with or without extension_type


* JSONB
  * build jsonb_node from json_node and viceversa
  * add hasher to hash some attribute nestedly